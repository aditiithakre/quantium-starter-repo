# Quantium starter repo
This repo contains everything you need to get started on the program! Good luck!
## Setup
This project uses a Python virtual environment. The `venv/` directory is excluded from version control as per best practice.
